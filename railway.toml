[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build"
watchPatterns = ["src/**", "client/**", "package.json", "tsconfig.json"]

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
